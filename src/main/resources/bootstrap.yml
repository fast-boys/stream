server:
  port: 8082

spring:
  config:
    import: vault://
  application:
    name: application
  cloud:
    vault:
      authentication: TOKEN
      token: ${VAULT_TOKEN}
      kv:
        enabled: true
        backend: spring
        default-context: application
      uri: ${VAULT_ADDR}
